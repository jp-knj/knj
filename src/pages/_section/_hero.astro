<section id="hero" class="content-section hero">hero</section>

<style>
    .content-section {
        --section-gap: 3rem;
        --section-padding: 3rem;

        box-sizing: border-box;
        display: grid;
        gap: var(--section-gap);
        min-block-size: 100%;
        padding: var(--section-padding);
        border-block-end: 1px solid var(--color-ink);
    }
</style>
